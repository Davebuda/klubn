# klubn — Project CLAUDE.md

@AGENTS.md

## Routing
Route every task to the strongest skill / agent / tool — see the global router in `~/.claude/CLAUDE.md`.
Full catalogs live in the `omc-reference` skill. Global standards auto-load from `~/.claude/rules/` on matching files.

## This project
- Framework: React (Vite) | Runtime: node
- See `AGENTS.md` for stack, commands, and dependencies.
