# Decision Log

Architectural decisions for **KlubN** (codebase identity `DJDiP`).

One file per significant decision. Format: `YYYY-MM-DD-[slug].md`.
Each entry records the **WHY** and the **rejected alternatives** — the code already shows the WHAT.

Use `md-sync capture` (or `/md-sync`) to add entries from a Claude Code session.

## Entries
- [2026-06-06 — n8n ingest idempotency via EventKey](./2026-06-06-n8n-ingest-idempotency.md)
